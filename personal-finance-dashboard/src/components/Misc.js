import React from 'react';

const Misc = () => {
    return (
        <div>
            <h3>MISC</h3>
            <p>Can be whatever we want; we can add more cells if needed</p>
        </div>
    );
};

export default Misc;
